input:
  redact_pii: true
  block_on_injection: true

output:
  redact_pii: true
  block_credentials: true

tools:
  allowed:
    - get_user_info
    - search
    - execute_shell
    - query_database
    - get_weather

  blocked:
    - delete_record

logging:
  enabled: true